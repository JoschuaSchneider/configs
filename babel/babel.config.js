/**
 * Minimal babel configuration using babel env preset,
 * For testing and use with react, add @babel/preset-react to presets
 */

const presets = ["@babel/preset-env"]

module.exports = { presets }
